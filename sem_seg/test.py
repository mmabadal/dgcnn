import os
import numpy as np
import copy
 
if __name__=='__main__':

    z = 1


'''
    for pipe
        get start and end point
        get vector
        for start and end
            point at + width/2 + margin in + orthogonl direction (margin ~ 5cm)
            point at + width/2 + margin in - orthogonl direction (margin ~ 5cm)

    reproject points to 2d



''' 
